/** @deprecated Import from the package root (`compress` / `decompress`) instead. */
export {
  compress,
  decompress,
  compressWithOpenZL,
  decompressWithOpenZL,
  checkCLIAvailable,
  resetCLICache
} from './core/engine.js';
